opencv-python
numpy
supervision

roboflow
inference
sports @ git+https://github.com/roboflow/sports.git
torch
transformers
umap-learn
fastapi
uvicorn[standard]
python-dotenv
sqlalchemy>=2.0,<3
alembic>=1.13,<2
python-multipart>=0.0.9

# Optional: needed by train_xg.py (xG model training on StatsBomb open data)
requests
pandas
scikit-learn

# Optional: needed only for --video URL/YouTube input (lazy-imported)
yt-dlp

# Development / API and analytics tests
pytest>=8,<9
httpx>=0.27,<1
