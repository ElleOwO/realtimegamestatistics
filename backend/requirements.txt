# ============================================================================
# RTGS Backend Dependencies
# ============================================================================
# Minimal set for backend/soccer_analytics.py (webcam monolith):
#   Roboflow detection + SiGLIP team classification + pitch projection + Voronoi
#
# This file intentionally drops the FastAPI/WebSocket/Redis/RTSP/XGBoost/Gemini
# stack that previously lived here. Re-add packages as the backend is rebuilt.

# ── Computer Vision ───────────────────────────────────────────────────────
opencv-python
numpy
supervision                  # Detection helpers, ByteTrack, annotators

# ── Roboflow Models (player/field detection, team classification) ─────────
roboflow                     # Dataset management (also used by merge_and_upload.py)
inference                    # Roboflow inference server client (get_model)
sports @ git+https://github.com/roboflow/sports.git
                             # TeamClassifier, ViewTransformer, SoccerPitchConfiguration

# ── Team classifier transitive deps (SiGLIP + UMAP) ───────────────────────
# sports.TeamClassifier needs these; listed explicitly so they don't go missing.
transformers                 # SiGLIP embeddings (pulls torch transitively)
umap-learn                   # Dimensionality reduction in TeamClassifier.fit()
